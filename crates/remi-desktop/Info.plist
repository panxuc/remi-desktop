<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Merged into the bundle's generated Info.plist by tauri-cli (it looks for this file beside
  tauri.conf.json). Only `cargo tauri build` merges it — `cargo run -p remi-desktop` does not,
  so the dev loop always has a Dock icon and the bundle never does.

  LSUIElement: a pet is not an application in the Dock/Cmd-Tab sense. Her menu — including Quit —
  is reached by right-clicking her or through the menu bar icon (`tray.rs`), neither of which
  needs a Dock tile, so this is the key that makes the bundle behave like a pet rather than an app
  with a transparent window.

  NSHighResolutionCapable: without it the webview is scaled up from 1x and the Spine art is
  visibly soft on every Mac made in the last decade.
-->
<plist version="1.0">
<dict>
  <key>LSUIElement</key>
  <true/>
  <key>NSHighResolutionCapable</key>
  <true/>
</dict>
</plist>
